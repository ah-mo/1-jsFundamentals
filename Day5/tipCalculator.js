function tipCalculator(bill, tipPercent) {
    let tip = (bill * (tipPercent / 100));
    console.log(tip);
    return tip;
}

tipCalculator(50, 15);

let b = ((100*4 ) + (50*7) + (20*32));
console.log(b);